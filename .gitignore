node_modules/
.DS_Store
*.log

# Derived: rebuilt on every refresh
data/events.json

# Captured live sources are local working files, not fixtures to commit
test/fixtures/live-*

# The generated calendar is derived output
data/openice.ics

# Built by the publish workflow
dist/
