{
  "name": "northshore-ice-finder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "refresh": "node server.mjs --refresh",
    "test": "node --test \"test/*.test.mjs\"",
    "capture": "node scripts/capture.mjs"
  }
}
